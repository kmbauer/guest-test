{
    "lwc": { "modules": [{ "dir": "$rootDir/src/modules" }] },
    "bundleConfig": { "exclude": ["lwc"] },
    "routes": [
        {
            "id": "example",
            "path": "/",
            "rootComponent": "example/app",
            "bootstrap": {
                "services": ["example/stripVersionHook"]
            }
        }
    ]
}
